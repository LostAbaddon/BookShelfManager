{
	// 书所存放的路径，以及需要被提出的路径，后缀名为path_config
	path	: 'main',
	// 文件类型匹配，后缀名为pattern_config
	pattern	: 'main',
	// 整理后的文件存放位置，后缀名为.shelf_config
	shelf	: 'main',
	// 文档属性，后缀名为prop_config
	prop	: 'main',
	// 文档Tag，后缀名为tag_config
	tag		: 'main',
}